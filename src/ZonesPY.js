export default class ZonesPY {
    constructor(id, zoneName,duration,guests, popularity,profit) {
      this.id = id;
      this.zoneName = zoneName;
      this.duration = duration;
      this.guests=guests;
      this.popularity=popularity;
      this.profit=profit;
    }
  }